<h1>Bucket List</h1>

<% if @items.any? %>
  <ul>
    <% @items.each do |i| %>
      <li>
        <%= link_to i.name, item_path(i) %>
      </li>
    <% end %>
  </ul>
<% else %>
  <p>Add an item!</p>
<% end %>
<%= link_to 'Add new item' %>
