<h1>Bucket List</h1>

<% if @list.items %>
  <ul>
    <% @list.items.each do |i| %>
      <li>
        <%= link_to i.name, item_path(i) %>
      </li>
    <% end %>
  </ul>
<% else %>
  <p>Add an item!</p>
<% end %>
<%= link_to 'Add new item', new_list_item_path(@list)%>
